// Configuración general del Observatorio
// Editable desde el panel de Administración (los cambios se guardan en localStorage).
// Para hacerlos permanentes, actualiza este archivo.

const configuracion = {
  version: '1.3.1',
};

export default configuracion;
